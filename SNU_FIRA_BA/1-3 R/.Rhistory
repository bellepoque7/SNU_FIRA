x <- c(5,12,13,12)
xf = factor(x)
xf
x <- c(5,12,13,12)
xf = factor(x)
xf
str(x) #내부 구조를 보여주는 함수
unclass(xf) #클래스의 속성들을 다뻄
as.numeric(xf) #numeric으로 casting
cand = c("Jack", "Jill", "nick", "richard")
table(a)
cand = c("Jack", "Jill", "nick", "richard")
a = c("Jack","nick","richard","Jack","richard")
table(a)
af = factor(a,levels = cand)
str(af)
table(af)
af = factor(a,levels = cand)
table(af) # a에는 Jill이 없으나 levels를 지정해주어
af
source('~/GitHub/SNU_BDI_-JJJ-R/05Factor.R', encoding = 'UTF-8')
